import { Server, ShieldCheck, GitBranch, Database, Zap, Layers, Boxes } from 'lucide-react';

export const iconFor = {
  server: Server,
  shield: ShieldCheck,
  git: GitBranch,
  db: Database,
  zap: Zap,
  layers: Layers,
  boxes: Boxes,
};
